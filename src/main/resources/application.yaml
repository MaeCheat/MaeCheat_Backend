spring:
  application:
    name: MaeCheat
  datasource:
    url: jdbc:mysql://localhost:3306/maecheat
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true

nexon:
  api:
    key: ${NEXON_API_KEY}